{% extends 'base.html.twig' %}

{% block title %}Author{% endblock %}

{% block body %}
    <h1>Author</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ author.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ author.name }}</td>
        </tr>
        <tr>
            <th>Sex</th>
            <td>{{ author.sex }}</td>
        </tr>
        <tr>
            <th>Date of birth</th>
            <td>{{ author.birth_date ? author.birth_date|date('Y-m-d H:i:s') : '' }}</td>
        </tr>
        <tr>
            <th>Nationality</th>
            <td>{{ author.country_of_origin }}</td>
        </tr>

        <tr>
            <th>Books</th>
            <td>
                {% for book in author.books %}
                    <a href="{{ path('book_show', {'id':book.id}) }}" class="badge bg-secondary">{{ book.title }}</a>
                {% endfor %}
            </td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('author_index') }}" class="btn btn-primary mt-3 mb-3 btn-sm"><i class="fas fa-arrow-alt-circle-left"></i> back to list</a>

    {% if is_granted('ROLE_ADMIN') %}
        <a href="{{ path('author_edit', {'id': author.id}) }}" class="btn btn-secondary mt-3 mb-3 btn-sm"> <i class="fas fa-edit"></i> edit</a>

         {{ include('author/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}