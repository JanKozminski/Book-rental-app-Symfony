{% extends 'base.html.twig' %}

{% block title %}{{ 'author.new'|trans }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-8 offset-2 login p-4 mb-4 mt-4">
            <h1 class="mt-2 mb-4">{{ 'create.new.author'|trans }}</h1>

            {{ include('author/_form.html.twig') }}

            <a href="{{ path('author_index') }}" class="btn btn-primary mt-3 mb-3 btn-sm"><i class="fas fa-arrow-alt-circle-left"></i> {{ 'action.back_to_list'|trans }}</a>

        </div>
    </div>


{% endblock %}